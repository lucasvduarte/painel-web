import React from 'react';
import styled from "styled-components";
import Avatar from '@material-ui/core/Avatar';

export const AvatarStyled = styled(({ ...props }) => (
    <Avatar aria-label="recipe"  {...props} />
))` 
   && { 
        background-color: #f44336;
    }
`;